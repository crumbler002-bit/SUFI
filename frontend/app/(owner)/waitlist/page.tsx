"use client";

import { Layout } from "@/components/Layout";

export default function WaitlistPage() {
  return (
    <Layout title="Waitlist" subtitle="Active waitlist entries">
      <div className="flex items-center justify-center h-64 text-[#5a5480] font-mono text-[13px]">
        Waitlist page — coming soon
      </div>
    </Layout>
  );
}
